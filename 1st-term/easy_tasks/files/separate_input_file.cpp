
/*
#include <iostream>

int main() {
    std::cout << R"(C++ is a general-purpose programming language with a bias towards systems programming that
  - is a better C
  - supports data abstraction
  - supports object-oriented programming
  - supports generic programming.)" << std::endl;
    return 0;
}

*/

/*
#include <iostream>
#include <vector>
#include <string>

int main() {
    std::vector<std::string> lines = {
        "C++ is a general-purpose programming language with a bias towards systems programming that",
        "  - is a better C",
        "  - supports data abstraction",
        "  - supports object-oriented programming",
        "  - supports generic programming."
    };

    for (const auto& line : lines) {
        std::cout << line << std::endl;
    }

    return 0;
}

*/



/*
Пример 1.  Входные данные — те же, что и в предыдущем примере.

Предположим, у тебя есть строка с разделителями в виде символа новой строки \n, 
и тебе нужно разделить её на отдельные строки.

*/


/*
Пример 2: Входные данные — произвольные.

Теперь предположим, что ты не знаешь, как будут выглядеть входные данные, и тебе нужно разделить их на строки

Объяснение:

    Мы читаем входные данные от пользователя построчно.
    Добавляем каждую строку в одну большую строку input с разделителями \n.
    Затем используем std::stringstream для разделения этой строки по символу новой строки.
    Все строки добавляются в вектор std::vector<std::string>, после чего выводятся на экран.
*/


/*
Пример 3: Чтение данных из файла


*/

/*
Пример 4: Чтение из строкового потока:

*/

/*
Пример 5: Цикл вывода ссылок на константу типа auto

*/

/*
Пример 6: Чтение строк от пользователя через бесконечный цикл и через цикл с условием


*/

/*
Пример 7: Чтение строк из файла с обработкой случая, открылся ли файл, с построчным чтением, и с закрытием файла.
*/

/*
Пример 8: разделение неотформатированного текста из входного потока  
*/

/*
Пример 9: разделение текста из входного потока по ключевым словам
*/

/*
Пример 10: разделение форматированного текста из входного потока. Рассказ о разделителях текста.
*/

/*
Пример 11: разбор текста по длине строки

*/

/*
Пример 12: построчное чтение текста с посимвольным разделением
*/

/*
Пример 13: разбиение по длине
*/

/*
Пример 14: чтение файла целиком в одну строку

*/

