#include <vector>
#include <array>


/// А если я хочу передать РЕАЛЬНЫЙ массив, а не std array

// может, надо передавать указатель?


std::vector<int> find_array_duplicates(const std::array<int>& other) {
    if (){}



}