

16. Итераторы. Концепция, требования к forward итератору. 
17. Итераторы. Концепция, требования к forward итератору. 

- Последовательные контейнеры в STL.
- Ассоциативные контейнеры в STL


## Итераторы

### [](#%D0%BF%D1%80%D0%B5%D0%B8%D0%BC%D1%83%D1%89%D0%B5%D1%81%D1%82%D0%B2%D0%B0)Преимущества


Итераторы используют в ассоциативных контейнерах, где не применима индексация, в отличие от последовательных контейнеров.

Итератор противопоставлен индексации, основанной на счетчике цикла.
Индексация применима для последовательных контейнеров, но не для ассоциативных.

В частности, итератор используют для структур данных с медленным произвольным доступом или вообще без поддержки такового (например, список или дерево). 
Итераторы предоставляют возможность последовательного перебора любых структур данных, поэтому делают код более читаемым, удобным для повторного использования и менее чувствительным к изменениям структур данных.


### [](#%D1%82%D0%B8%D0%BF%D1%8B-%D0%B8%D1%82%D0%B5%D1%80%D0%B0%D1%82%D0%BE%D1%80%D0%BE%D0%B2)Типы итераторов

##### [](#%D0%B8%D1%82%D0%B5%D1%80%D0%B0%D1%82%D0%BE%D1%80-%D0%B2%D0%B2%D0%BE%D0%B4%D0%B0)Итератор ввода

`Input iterator` предназначен только для однократного чтения (ввода) последовательности значений.

```
Value value = *it++; // прочитать следующее значение, it - итератор
```

Итератор можно передвигать на одну позицию вперед (инкремент) и разыменовывать (операции * и ->), получая доступ к текущему значению. Итераторы можно сравнивать между собой на равенство и неравенство.


##### [](#%D0%BE%D0%B4%D0%BD%D0%BE%D0%BD%D0%B0%D0%BF%D1%80%D0%B0%D0%B2%D0%BB%D0%B5%D0%BD%D0%BD%D1%8B%D0%B9-%D0%B8%D1%82%D0%B5%D1%80%D0%B0%D1%82%D0%BE%D1%80)Однонаправленный итератор

`Forward iterator` является расширением концепции “итератор ввода”, т.е. предоставляет возможности итератора ввода (и, возможно, но не гарантированно, итератора вывода). Кроме того, однонаправленный итератор допускает многократное чтение и запись линейной последовательности, по которой можно двигаться только в одну сторону (как по односвязному списку — “вперёд” с помощью операции ++).


Однонаправленные итераторы
Аналогичны итераторам ввода, но отличаются тем, что по диапазонам данных, кото-
рые они представляют, вы можете проитерировать несколько раз. В качестве примера
такого итератора приведем std::forward_list. По такому списку можно проитериро-
вать только вперед, но не назад, однако это можно сделать требуемое количество раз.



## Использование в контейнерах

Формально, итератор не обязан итерироваться по контейнерам или итерироваться вообще по чему либо. Итератор может делать любую работу.

Каждый контейнерный класс имеет 4 основных метода для работы с оператором =:

- **begin()** возвращает итератор на начало элементов контейнера.

- **end()** возвращает итератор на элемент после последнего элемента в контейнере.    

- **cbegin()** возвращает константный (на чтение) итератор на начало элементов контейнера.

- **cend()** возвращает константный (на чтение) итератор, на элемент после последнего в контейнере.    


## Контейнеры последовательности

Последовательные контейнеры поддерживают указанный пользователем порядок вставляемых элементов.

Контейнер `vector` ведет себя как массив, но может автоматически увеличиваться по мере необходимости. Он поддерживает прямой доступ и связанное хранение и имеет очень гибкую длину. По этим и многим другим причинам контейнер `vector` является наиболее предпочтительным последовательным контейнером для большинства областей применения. Если вы сомневаетесь в выборе вида последовательного контейнера, начните с использования вектора. Дополнительные сведения см. в разделе [`vector` "Класс](https://learn.microsoft.com/ru-ru/cpp/standard-library/vector-class?view=msvc-170)".

Контейнер `array` имеет некоторые сильные стороны `vector`, но длина не так гибка. Дополнительные сведения см. в разделе [`array` "Класс](https://learn.microsoft.com/ru-ru/cpp/standard-library/array-class-stl?view=msvc-170)".

Контейнер `deque` (двусторонняя очередь) обеспечивает быструю вставку и удаление в начале и в конце контейнера. Он делится преимуществами `vector`случайного доступа и гибкой длины, но не является непрерывным. Дополнительные сведения см. в разделе [`deque` "Класс](https://learn.microsoft.com/ru-ru/cpp/standard-library/deque-class?view=msvc-170)".

`list` Контейнер — это двунаправленный список, который обеспечивает двунаправленный доступ, быстрые вставки и быстрые удаления в любом месте контейнера, но вы не можете случайно получить доступ к элементу в контейнере. Дополнительные сведения см. в разделе [`list` "Класс](https://learn.microsoft.com/ru-ru/cpp/standard-library/list-class?view=msvc-170)".

Контейнер `forward_list` — однонаправленный список. Это версия контейнера `list` только с доступом в прямом направлении. Дополнительные сведения см. в разделе [`forward_list` "Класс](https://learn.microsoft.com/ru-ru/cpp/standard-library/forward-list-class?view=msvc-170)".


## Ассоциативные контейнеры

Отображения / множества, основанные на порядке элементов. Обычно реализуются в виде красно-чёрных деревьев по ключам.

В ассоциативных контейнерах элементы вставляются в предварительно определенном порядке — например, с сортировкой по возрастанию. Также доступны неупорядоченные ассоциативные контейнеры. Ассоциативные контейнеры можно объединить в два подмножества: сопоставления (set) и наборы (map).

Контейнер `map`, который иногда называют словарем, состоит из пар "ключ-значение". Ключ используется для упорядочивания последовательности, а значение связано с ключом. Например, `map` может содержать ключи, представляющие каждое уникальное ключевое слово в тексте, и соответствующие значения, которые обозначают количество повторений каждого слова в тексте. `map` — это неупорядоченная версия `unordered_map`. Дополнительные сведения см. в разделе ["Класс и](https://learn.microsoft.com/ru-ru/cpp/standard-library/map-class?view=msvc-170) [`unordered_map` класс](https://learn.microsoft.com/ru-ru/cpp/standard-library/unordered-map-class?view=msvc-170)".`map`

`set` — это контейнер уникальных элементов, упорядоченных по возрастанию. Каждое его значение также является и ключом. `set` — это неупорядоченная версия `unordered_set`. Дополнительные сведения см. в разделе ["Класс и](https://learn.microsoft.com/ru-ru/cpp/standard-library/set-class?view=msvc-170) [`unordered_set` класс](https://learn.microsoft.com/ru-ru/cpp/standard-library/unordered-set-class?view=msvc-170)".`set`

Контейнеры `map` и `set` разрешают вставку только одного экземпляра ключа или элемента. Если необходимо включить несколько экземпляров элемента, следует использовать контейнер `multimap` или `multiset`. Неупорядоченные версии этих контейнеров — `unordered_multimap` и `unordered_multiset`. Дополнительные сведения см. в разделе ["Класс", "Класс", "Класс](https://learn.microsoft.com/ru-ru/cpp/standard-library/multimap-class?view=msvc-170)", [`unordered_multimap``multiset`](https://learn.microsoft.com/ru-ru/cpp/standard-library/multiset-class?view=msvc-170)["Класс](https://learn.microsoft.com/ru-ru/cpp/standard-library/unordered-multimap-class?view=msvc-170)" и [`unordered_multiset` "Класс".](https://learn.microsoft.com/ru-ru/cpp/standard-library/unordered-multiset-class?view=msvc-170)`multimap`

Упорядоченные контейнеры map и set поддерживают двунаправленные итераторы, а их неупорядоченный аналоги — итераторы с перебором в прямом направлении. Дополнительные сведения см. в разделе [Итераторы](https://learn.microsoft.com/ru-ru/cpp/standard-library/iterators?view=msvc-170).

