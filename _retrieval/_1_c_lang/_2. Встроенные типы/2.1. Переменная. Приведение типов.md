
- Переменная – это совокупность байтов, за-
нимающая непрерывный участок виртуаль-
ного адресного пространства и используемая
для хранения некоторого значения.

Размещение переменных в виртуальном ад-
ресном пространстве – прерогатива компи-
лятора и менеджера памяти языка C, а так-
же загрузчика операционной системы Linux.
Адрес первого байта участка адресного про-
странства, занимаемого переменной, назы-
вается адресом переменной. Все переменные
имеют разные адреса.


Обращение к переменной – это чтение или запись её значе-
ния. Чтобы обратиться к переменной, нужно знать её адрес.
Переменные создаются и уничтожаются во время работы
программы. Время жизни переменной – это интервал вре-
мени от создания до уничтожения переменной.

Имя переменной – это символическое обозначение адреса
переменной в исходном тексте программы.
Не каждая переменная имеет имя: например, обращение к
динамической переменной, созданной во время выполнения
программы, осуществляется через указатель, содержащий
адрес этой динамической переменной.
В машинном коде имена переменных отсутствуют: вместо
них компилятор языка C вставляет адреса переменных.
Имя переменной записывается в виде идентификатора –
последовательности латинских букв, цифр и знаков подчёр-
кивания, не начинающейся с цифры.
Следует иметь в виду, что в идентификаторах учитывается
регистр букв.

Классификация по времени жизни


Глобальные переменные – это переменные, время жизни ко-
торых длится от момента запуска программы до домента
завершения работы программы. К глобальным переменным
потенциально можно обращаться из любой функции про-
граммы.
Локальные переменные – это переменные, которые автома-
тически создаются при вызове функции и уничтожаются
при завершении работы функции. В локальных перемен-
ных хранятся промежуточные результаты вычислений. Как
правило, обращения к локальным переменным происходят в
функции, которой эти локальные переменные принадлежат.
Формальные параметры функции – это локальные пере-
менные, в которые записываются передаваемые при вызове
функции значения.
Динамические переменные – это переменные, создание и
уничтожение которых осуществляется во время работы про-
граммы путём обращения к менеджеру памяти.

Объявление переменной сообщает компилятору имя и тип
переменной. Оно играет двоякую роль
- во время компиляции программы оно определяет смысл
операций, применяемых к переменной;
- во время выполнения программы объявление обеспечи-
вает резервирование участка виртуального адресного
пространства для хранения значения переменной


Место объявлений переменных
в ¾шаблоне¿ программы

Объявление переменной должно быть записыно до первого
оператора, в котором эта переменная используется.
Локальные переменные объявляются внутри фигурных ско-
бок, ограничивающих тело функции. Одним из способов
объявить глобальную переменную является вынесение её
объявления на верхний уровень программы (другой способ –
объявление с ключевым словом static).

Неинициализированные переменные
При объявлении переменной можно не указывать её началь-
ного значения.

Неинициализированные глобальные переменные по умолча-
нию получают нулевое значение. Что касается неинициали-
зированных локальных переменных, то при создании такой
переменной в память ничего не пишется, и значением пере-
менной становится ¾мусор¿, который случайно оказался в
выделенном для неё участке памяти.
Распространённая ошибка при программировании на C –
это неявное предположение, что неинициализированная ло-
кальная переменная имеет нулевое значение. Она особенно
коварна тем, что проявляется не при каждом запуске про-
граммы.





## 2.1 Переменная. Приведение типов.

- С чем работает программа, с чем алгоритм, а с чем код?
	- алгоритм не привязан к ЯП
	- код - привязан к ЯП
	- программа - исполняемая версия алгоритма на основе кода.
	- основа программы закладывается в коде.


- Кто из них работает с числами, операторами, переменными, значениями?
	- код


- если их много, какие корнер-кейсы? 
	- хранение. 
	- как их хранить, представлять в компе? для этого есть специальные типы?

- если число малое, низкая точность?


- Зачем переменные, если есть числа?
	- А если я захочу переиспользовать? Или изменять число, мне нужно промежуточное значение для хранения (?? где). 

- Как охарактеризовать переменную? Что про неё надо знать?
	- Что
	- Где
	- Как
- Что еще можно назвать временным хранилищем, помимо переменной?
- чем переменная отличается от массива, если и то и другое временное хранилище?
	- а есть ли тогда постоянное хранилище? или нет ничего постоянного?
	- Почему же тогда диски хранят постоянно, если они основаны на b-tree, а b-tree основан на указателях? Но видимо это b-tree не на динамической памяти, а на постоянной, если при выключении операционной системы данные не затираются? а как такая память будет называться?

- Переменные. Типы и виды. Отличие от типов и видов данных. От абстрактных типов данных...
	- Локальные
	- Глобальные
	- Статические
	- Константные
	- целочисленные
	- строковые

