

Можно использовать ее для оптимизации копирований
и присваиваний, перемещая внутренние ресурсы из исходного объек­та в объект назначения вместо копирования этого содержимого. 

Используется при условии, что исходный объект не нуждается в своем внутреннем значении или состоянии (потому что все равно будет отброшен).

## Правило 5 


```c++
template <typename T>
void swap(T& first, T& second) {
	T tmp = std::move(first);
	first = std::move(second);
	second = std::move(tmp);
}

ресурсы из объектов справа перемещаются налево.
сами объекты становятся пустыми, но не исчезают.
```

А если просто вызвать std::move(object) - ничего не произойдет.
(забегая вперед, это статик каст)


***
Автоматическая генерация и правило пяти.
Мув-конструктор генерируется автоматически если нет user-declared move-constructor и:

1. нет user-declared-copy constructor
2. user-declared operator assignment copy
3. user-declared move-operator copy
4. user-declared destructor

С move-operatorom аналогично.

Правило пяти, в отличие от правила трех, выполняется компилятором.
Правило трех компилятором генерируется.

##### Резюме

- move-семантика нужная для:
    
    - скорости, чтобы избавиться от лишних копирований
    - корректности, чтобы можно было возвращать некопируемые объекты
- категории выражений, которые желательно понимать и различать обычным смертным: `lvalue`, `xvalue`, `prvalue`
    
- после выполнения move-операции над классом он остаётся в unspecified valid state (за некоторыми отдельно прописанными исключениями типа `std::unique_ptr`, `std::shared_ptr`, кастомных классов и т.п.)


```c++
int main() {
	int&& x = 0; // OK
	int&& y = x; // CE
	int&& y = std::move(x); // OK
}
```

***

move-semantics

```c++
template <typename T>
void swap(T& first, T& second) {
  T tmp = first;
  first = second;
  second = tmp;
}
```


Во всех трех строках происходит копирование. Если бы мы вызвали такую функцию от какого-нибудь вектора, то мы бы сделали три линейных прохода.


Перепишем std::swap
Для большинства стандартных типов он работает за О(1)
Неформально: посмотрим на эту строчку T tmp = std::move(first); Здесь все "ресурсы" объекта first были отданы объекту tmp. Не скопированы, а просто перемещены.

Сразу важные замечания:

```c++
template <typename T>
void swap(T& first, T& second) {
  T tmp = std::move(first);
  first = std::move(second);
  second = std::move(tmp);
}
```



Конструктор std::string вызовется дважды (потому что push_back вызывает конструктор). Опять лишнее копирование!

```c++
int main() {
  vector<std::string> v;
  v.push_back(std::string("test"));
}
```

Эта проблема решается с помощью метода emplace_back:

```c++
int main() {
  vector<std::string> v;
  v.emplace_back("test");
}
```

Метод emplace_back принимает аргументы, из которых потом конструирует объект:
```C++
template <typename... Args>
void emplace_back(const Args&... args) {
  if (size_ == capacity_) {
    reserve(2 * capacity_);
  }
  new(arr + sz) T(args...);
  size_ += 1;
}
```


```c++
std::unique_ptr<Person> create_person(std::unique_ptr<Head> head, std::unique_ptr<Body> body);

create_person(std::make_unique<Head>(), std::make_unique<Body>());
```


***
используем в конструкторах, в функциях для эффективной их реализации.
во избежание лишних копирований.

при этом мув семантика работает с понятиями рвалью и лвалью.
внутрь себя стд мув принимает рвалью.
а стд форвард?

