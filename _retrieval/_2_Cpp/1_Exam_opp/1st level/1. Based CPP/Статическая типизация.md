Статическая типизация - все типы должны быть известны на момент компиляции.
Статическое - на этапе компиляции, динамическое - в рантайме.

Почему точка плавающая?
Это то как типы в памяти хранятся.
Дабл - удвоенная точность.

Типы с плавающей точкой: мантисса и экспонента.
Экспонента - степень двойки, на которую умножают мантиссу, чтобы получить число.

Чем больше число по модулю, тем больше дискретность.

Сайз_т - беззнаковый целочисленный память, его достаточно для индексации любой ячейки массива. нужен для индексации. его хватит, чтобы все проиндексировать.

Стринг - это класс.
Для стд стринг последний символ строки - / 0 (нулль- терминированная строка, совместимость с сишной строкой).

Негарантированная ошибка - это во благо времени.

.ат(сайз_т) - ат - проверка выхода за границу (безопасная)..

Вектор нельзя складывать с другим вектором.

Пушбэк в векторе работает за О(1) амортизированное - потому что во время реаллокации работает за О(Н)

Поп бэк за О(1) и все остальное.

Шринк ту фит - заставляет дропнуть лишнюю память, если не используется.

typedef + define - legacy

в одном и том же скоупе 2 раза определять одно и то же нельзя.


а++ - РВАЛЬЮ - создает новое значение, и оно возвращается. вернули то что было.
++а - лвалью - результатом является новое. лвалью можно что-то присвоить.

постфиксные операции приоритетнее
++(а++)

++++а - можно
а+++++В
нельзя

потому что нельзя а++++

это определяет парсер (на уровне лексики)

а - первая лексема.
а+ - нет такой лексемы.
компилятор отщипнет а++

а+++в - можно (а++) + в

тернарный оператор - три аргумента.

```c++
int * a[10]; - array of ten pointers to int (first we read from right side).
int (*b)[10]; - указатель на массив из 10 интов.

void (*pf)(int) // pointer to function which takes int, returns void

```


reinterpret cast - cast к любому типу.
но нельзя создать новый объект.
только посмотреть на старый по-новому.

конст каст - нужен, когда надо отменить константность.

