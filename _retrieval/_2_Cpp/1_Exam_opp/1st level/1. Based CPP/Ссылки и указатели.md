Ссылки. Lvalue и rvalue ссылки. Отличие указателей и ссылок. Операции с ссылками. Константные ссылки и константные указатели. Move семантика.

***



Указателей на ссылку нет, потому что ссылка нигде не хранится.

Нельзя объявить массив ссылок, потому что это псевдонимы, а не объекты.
Нельзя объявить ссылку на ссылку (не rvalue-ссылка)

Константность означает, что объект больше нельзя изменять (создали единожды).

```
lvalue имеет адрес, к которому может получить доступ программа.

Примеры lvalue:

- имена переменных
- элементы массива
- вызовы функций, члены класса

```


```
Неявные преобразования типов.

Если выражение содержит операнды различных встроенных типов и нет явного приведения, компилятор использует встроенные стандартные преобразования для преобразования одного из операндов таким образом, чтобы типы соответствовали.

Есть сужающие и расширяющие преобразования.

Тип void.
Описывает пустой набор значений.
Используется для объявления функций, которые не возвращают значения или объявляют универсальные указатели на произвольные типизированные данные.

Объявления и определения.

Программа состоит из разного рода сущностей.
Перед их использованием каждую из них нужно объявить.
Объявление казывает уникальное имя сущности, а также сведения о еге типе и др.
В с++ точка объявления имени - это точка, в которой они становятся видимы компилятору.
```


# Ссылки на r-значение


Для обеспечения операции пересылки введены ссылки на r-значение.
_Ссылка на r-значение_  должна быть связана с r-значением. 
Ссылку на r-значение получают с использованием символа &&, а не &. 

У ссылок на r-значение есть важное свойство — они могут быть связаны только с тем объектом, который будет удален.
В результате можно "перемещать" ресурсы от ссылки на r-значение в другой объект.

***

Указателей на ссылку НЕТ, потому что ссылка нигде не хранится.
Нельзя объявлять массив ссылок. Потому что ссылка это псевдоним, а не объект.

Лвалью имеет адрес, к которому может получить доступ программа.

примеры лвалью: имена переменных, элементы массива, вызовы функций, члены класса.

Польза указателей:
- используются для итерации по массиву.
- это единственный способ динамического выделения памяти

указатель можно разыменовать для извлечения значений, хранимых по адресу памяти.
оператор разыменования вытаскивает значение.

