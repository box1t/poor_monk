Указатель std::shared_ptr используется для управления ресурсами путем совместноrо владения, т.е. объект, на который указывает shared_ptr, уничтожится только после того, как не останется ни одного shared_ptr, ссылающегося на него.

Подсчет ссылок в shared_ptr построен с использованием атомарного счетчика. Можно безопасно использовать указатели на один и тот же объект из разных потоков. Во всяком случае, не стоит беспокоиться о подсчете ссылок. Потокобезопасность самого объекта – другая проблема, и о ней надо заботится отдельно.

Shared_ptr
У объекта может быть несколько владельцев. Это называется разделяемым владением и работает следующим образом: за удаление объекта ответственен последний владелец.  `std::shared_ptr` для реализации владения использует подсчёт ссылок.

Как и `unique_ptr`, `shared_ptr` поддерживает операции `operator*`, `operator->`, `reset`, `get`. Операции `release` у `shared_ptr` нет, потому что могут быть другие `shared_ptr`, ссылающиеся на этот объект.

`shared_ptr` внутри себя хранит указатель на ссылаемый объект и указатель на счётчик ссылок. Поскольку счётчик ссылок у всех `shared_ptr`, ссылающихся на один и тот же объект, общий, то память под него аллоцируется динамически.


Объект удаляется (то есть его память освобождается), когда последний `std::shared_ptr`, владеющий ресурсом, уничтожается.

```c++
template<typename T>
class shared_ptr {
private:
	T* ptr;
	size_t* ref_counter;
public:
	shared_ptr() : ptr(nullptr), ref_counter(nullptr) {}

	shared_ptr(T* ptr) : ptr(ptr) {
	    if (ptr == nullptr) {
	        return;
	    }
	    try {
	        ref_counter = new size_t(1);
	    } catch (...) {
	        delete ptr;
	        throw;
	    }
	}
	shared_ptr(shared_ptr const& other) 
	: ptr(other.ptr), ref_counter(other.ref_counter) {
	    if (ptr == nullptr) {
	        return;
	    }
	    ++ref_counter;
	}
	~shared_ptr() {
	    if (ptr == nullptr) {
	        return;
	    }
	    if (--*ref_counter == 0) {
	        delete ref_counter;
	        delete ptr;
	    }
	}

};
```


Преимущества:

- совместное управление объектом
- копируемый и перемещаемый
- можно работать в условиях многопоточности


```c++
##### std::shared_ptr IR

Перед тем как показать ответ, попробуйте ответить на следующие вопросы:

Объект должен умереть, тогда и только тогда когда умрёт **последний** его владелец. Какую информацию придётся хранить?

Где эту информацию хранить? Как насчёт такого примера:

std::shared_ptr<T> a(new T);
std::shared_ptr<T> b = a;
std::shared_ptr<T> c = b;

a.reset();
b.reset();
```

```c++
##### make_shared - способ быстрее создавать shared_ptr



// 200 аллокаций на объекты + 1 аллокация на вектор
std::vector<std::shared_ptr<Cat>> herd;
herd.reserve(100);
for (int i = 0; i < 100; ++i)
    herd.push_back(std::shared_ptr<Cat>(new Cat()));


// 100 аллокаций на объекты + 1 аллокация на вектор
std::vector<std::shared_ptr<Cat>> herd;
herd.reserve(100);
for (int i = 0; i < 100; ++i)
    herd.push_back(std::make_shared<Cat>());
```

