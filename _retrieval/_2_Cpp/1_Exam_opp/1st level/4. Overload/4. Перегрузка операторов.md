Перегрузка операторов. Перегрузка оператора =. Move семантика. Запрет операторов копирования. Перегрузка ().
Функтор.

***
**Нельзя перегрузить**:

- Оператор для базовых типов. Только для кастомных.
- Нельзя менять АРНОСТЬ оператора.
- Нельзя заводить НОВЫЙ оператор.



- доступ через точку а.б
- доступ к члену класса через точку а.* б
- доступ к пространству имен а::б
- последовательный доступ а ; б
- почти все специальные операторы sizeof alignof typeid static_cast
- тернарный оператор а ? б : с

***

В языке C++ операторы реализованы в виде функций. Используя перегрузку функции оператора, вы можете определить свои собственные версии операторов, которые будут работать с разными типами данных (включая [**классы**](https://ravesli.com/urok-113-klassy-obekty-i-metody-klassov/)). 

Перегруженный оператор — это функция со специальным именем, состоящим из ключевого слова operator, сопровождаемого символом определяемого оператора. 


За исключением перегруженного оператора вызова функции, operator(), у перегруженного оператора не может быть аргументов по умолчанию (см. раздел 6.5.1).


Когда перегруженный оператор является функцией-членом, указатель this соответствует левому операнду. У операторов-членов на один явный параметр меньше, чем операндов.

# Оператор + через +=

```c++
struct Complex{

	double re = 0.0;
	double im = 0.0;
	
	Complex(double re) : re(re) {}
	Complex(double re, double im) : re(re), im(im){}
	Complex& operator+=(const Complex& other) {
		re += other.re;
		im += other.im;
		return *this;
	}

};

Complex operator+(const Complex& a, const Complex& b) {
	Complex result = a;
	result += b;
	return result;	
}

bool operator<(const Complex& a, const Complex& b) {
	return a.re < b.re || a.re == b.re && a.im < b.im;
}

bool operator>(const Complex& a, const Complex& b) {
	return b < a;
}
```





## Объяснение

### Способы перегрузки операторов
(бинарный) Оператор можно перегрузить двумя способами
- снаружи класса
- внутри класса


```c++
class Int {
public:
	Int(int a): a(a) {}

	Int operator+(const Int& other) {
		return Int(a + other.a);
	
	} 
private:
	int a;
};
В этом коде левым операндом всегда будет int.
Потому что левым операндом считается объект нашего класса.

int main() {
	Int first(10);
	Int second(5);
	first + second;

// У класса first вызовется оператор +, в который будет передан second.

	first.operator+(second); // эквивалентно

Возникает проблема.
Это значит, что левый операнд никак не может быть неявно приведен к классу.

	5 + first; // не скомпилируется.
	// язык не понимает, что 5 нужно привести к типу класса Int и суммировать.
}

Поэтому арифметические операторы лучше реализовывать вне класса.


Int operator+(const Int& lhs, const Int& rhs) {
	return Int(); 
	// Проблема: как сконструировать Int, если нет доступа к полю а?
}

Здесь появляется большое желание сделать наш оператор+ другом.
Но это не очень правильно.
В таком случае лучше реализовать геттер.
Ибо друг = проблема проектирования.

Решение:

class Int {
public:
	Int(int a) : a(a){}
	int GetValue() {return a;}
private:
	int a;
};

Int operator+(const Int& lhs, const Int& rhs) {
	return Int(lhs.GetValue() + rhs.GetValue());
}

int main() {
	Int first(10);
	5 + first; // Теперь такой код будет работать.
	// Потому что компилятор понимает, что может неявно скастить 5 к Int
	//, потому что конструктор не explicit, и это будет работать.
	
}

Но и этот вариант не стоит делать.
Сначала стоит реализовать +=, раз наш класс умеет в +
```

```c++
Реализация +=.
+= это бинарный оператор, левым объектом которого является объект нашего класса.
Поэтому мы реализуем его в теле класса, в отличие от +

class Int {
	Int(int a) : a(a) {}

	int GetValue() {return a;}
private:
	int a;
};

Int operator+(const Int& lhs, const Int& rhs) {
	Int copy = lhs;
	copy += rhs;
	return copy;
}

```



Почему не стоит выражать += через +?

Потому что + делает копию.
Он создает новый объект, и потому делает копию.
+= может обойтись без лишних копий.

```c++
- Почему так?

Int operator+(const Int& lhs, const Int& rhs) {
	Int copy = lhs;
	copy += rhs; 
	return copy;
}

Потому что здесь происходит NRVO.



- А не так:

Int operator+(const Int& lhs, const Int& rhs) {
	Int copy = lhs;
	return copy += rhs;
}

Потому что здесь не происходит NRVO


NRVO - named return value optimisation.
```

***

# Оператор присваивания и правило 3

```c++
int main() {
	Vector v(10, 5);
	Vector v2(3, 2);
	v = v2; // мы хотим, чтобы из вектора v2 скопировались значения в v
	// то есть создать копию таким образом
	// на самом деле здесь вызывается оператор присваивания, который
	// сгенерируется компилятором по умолчанию
}

```

Оператор присваивания должен принимать объект такого же типа (того же класса) и копировать из него.

```c++
class Vector{

	// Сначала надо понять, какое возвращаемое значение

/* Vector& */	operator=(const Vector& v) {}

// Возможно, захочется void. Но есть проблема:
// нельзя писать вот так: (v = v2) = v;
// хотя со стандартными типами так можно.
// в результате (v = v2) = v; вернется ссылка. значит, пишем ссылку

	Vector& operator=(const Vector& vec) {
		delete arr[]; // удаляем массив
		arr = new int[vec.size_];
		size = vec.size;
		memcpy(arr, vec.arr, size);
		return *this; // это указатель на себя	
	}

1. Но есть проблема: что если вектор был не пустой?
сначала нам нужно освободить ресурс!
иначе тот указатель, который уже хранился, будет утерян
, и мы не сможем очистить память.


Что же делает оператор присваивания копированием?
 - Он работает как деструктор: сначала освобождает захваченные объектом ресурсы
 - Затем он действует как конструктор копирования: копирует объекты, которые мы передали в себя.


};

2. Но есть вторая проблема:

int main() {
	Vector v(10, 3);
	Vector v2(2,4);
	v = v;
	// проблема: удаляем массив! а массив один и тот же
	// как же этого избежать?
}

Есть два способа решения:

2.1 

	Vector& operator=(const Vector& vec) {
		if (&v == this) {
			return *this; // и ничего делать больше не надо
		}
		delete arr[]; // удаляем массив
		arr = new int[vec.size_];
		size = vec.size;
		memcpy(arr, vec.arr, size);
		return *this; // это указатель на себя		
	}

2.2
	Vector& operator=(const Vector& vec) {
	Vector copy = v;
	swap(copy); // что это? это стандартный метод. его укажем приватным.
	return *this;
	
	}

private:
	void swap(Vector& v) {
		std::swap(arr, v.arr);
		std::swap(size, v.size);
	}

Итак, разбор 2.2:
- Создали полноценную копию объекта. 
- Мы можем сделать это, т.к. есть конструктор копирования
- Затем все поля текущего объекта поменял местами с переданными объектами.

```
