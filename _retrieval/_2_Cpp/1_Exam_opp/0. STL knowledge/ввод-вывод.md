

1. **Что эффективнее использовать: `'\n'` или `std::endl`?**  
    → `'\n'` эффективнее, так как `std::endl` вызывает очистку буфера вывода.
     - значит, вместе с std::cout можно использовать \n. но вообще и то, и другое.




