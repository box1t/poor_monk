
```
x_variable = int(input("Please, enter an integer: "))

как ввести число с клавиатуры?
```

```

как задавать функции?
а что на входе и на выходе? что сделать дефолтным?


почему питон позволяет менять местами подаваемые значения (при условии если мы укажем, какому значению приравниваем)?

и тип подставляемого значения выбирается автоматически?
а как у вадима?
есть же функции, где тип критичен?
да. а как это понять по коду?
а как это задать в коде?
а для чего используется его функция?

```




```
how many arguments print function gets?

как сделать обход цикла?
https://docs.python.org/3.10/tutorial/controlflow.html


- каковы основные методы словаря?
- чем отличаются get, items, keys, values?
- требует ли get выходного значения в случае, если ключ не найден в словаре?
	- что из этого при использовании print требует превращения в список и почему?



- как создать словарь на основе списка и ключа? 
	- а на основе списка ключей и списка словарей?

- чем отличаются pop, del?

- сортировать можно только список? а строку?
	- как строку разбить в список и отсортировать?
```

