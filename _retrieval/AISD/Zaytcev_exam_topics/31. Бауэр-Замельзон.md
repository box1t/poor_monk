

Это не самый ранний, а менее ранний. но тоже ранний. от раннего метода этот более молодой метод отличается использованием стека. за это время появились стеки в программировании. что и как стекирует этот метод? во первых, используются 2 стека, что в те года феноменально. а также используется таблица функций перехода. в реальности это что-то вроде машины тьюринга.



==стеки принято именовать Т и Е. Т - стек трансляции, Е - стек интерпретации выражения (evaluation - имеется в виду получение значения выражения). в таблице переходов задаётся функция для разбора выражения.==

==70 лет назад трансляторы уже писали. какие функции должен выполнять траснлятор при разборе выражения?==

==первая - засылаем операцию в стек трансляции==

==понятие символа - это знак со смыслом. звездочка - символ двуместной операции, за которой скрывается микрокод многократного сложения==

==что еще является символом строки? скобка! операнд! или какое-то число - константа. символом может быть знак, символизирующий операцию.==


==небрежность всегда оценивается с некоторым минусом==

==вторая функция - это фактически рутисхаузер. там мы ныряли в самую глубокую ямку. только здесь. получается в Т у нас операции, а операнды в Е.==

==надо проделать все с двумя разными выражениями. записывать ходы, как в шахматах. уже F2 требует цепочку действий. оказывается, там операции в стеке Т, а операнды в стеке Е.==

==F3 - если из стека операций убираем операцию, значит она выполнена и не нужна.==

==F4 - в зависимости от лексемы выбирается одна из функций.==



> на вершине стека операций отложены знаки. мы уже части выражений отработали, и у нас в стеке есть операции. на вершине стека закрывающая скобка не нужна - не закрывается.
> еще здесь введен доллар. доллар - это терминатор стека.
> если у нас на вершине стека находится доллар, т.е. стек пуст, и если входной поток (входная очередь) опустела - то 6. закончили.
> 5 - ошибка. ошибка приходит, когда у нас в стеке есть открывающая скобка, для которой нет закрывающей (при входном потоке).


> выражения просматриваем слева направо.
> если лексема операнд - переходит в стек.
> операнды все проходят в стек операндов.

> любой операнд потом переходит в стек evaluation.
> функция согласно таблице выбирается N1.
> операция поступает в стек операций.
> скобку мы тоже заносим в стек операций.
> скобка оформляет операции, её отдельно без знака не используем.
> операция плюс - это бинарная операция. 

> стек откладывает то, что нельзя сделать прямо сейчас.

> бауэр и замельс не любили бегать.
> они завели 2 стека, запрограммировали автомат.
> на экзамене не надо программировать стек! пользуемся готовым.
> мы должны предоставить именно алгоритм бауэра замельзона.
> запомнить заголовки процедура стека.
> быстродействие алгоритма не написано. мы должны прикинуть его сами.
> и записать формулу + некоторые разговоры.
> линейная сложность. проходим по строке и получаем это.
> а что такое сложность алгоритма?
> это количество элементарных операций (единиц памяти), которые потребляет алгоритм в зависимости от входного сообщения.
> но мало сказать правильно, нужно обосновать.
> линейную - сканируем длину входной очереди + операнды.
> а пространственная сложность? стек - структура динамическая, оценивается максимальная глубина стека. что-то линейное там напрашивается.
> это ранний стековый метод. но с двумя стеками.
> ранний - значит в нем что-то не то.
> наверное надо сравнивать с новыми алгоритмами



### Реализация на Си

```c



```

