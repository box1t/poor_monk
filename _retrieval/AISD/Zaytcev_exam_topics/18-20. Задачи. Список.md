

в списке числа идут от наименьшего к наибольшему.
пишем явно

# Слияние трех списков на СИ.

# 1. Вставка в список

```c

Iterator Insert(List* l, Iterator* i, const T t)
{
	Iterator res = { malloc(sizeof(struct Item)) };
	if(!res.node)
		return Last(l);
	res.node->data = t;
	res.node->next = i->node;
	res.node->prev = i->node->prev;
	res.node->prev->next = res.node;
	i->node->prev = res.node;
	l->size++;
	return res;
}

```



```c++
The new node is always added after the last node of the given Linked List.

we have to traverse the list till the end and then change the next to last node to a new node.

// Given a reference (pointer to pointer) to the head
// of a list and an int, appends a new node at the end
void append(Node** head_ref, int new_data)
{
	
	// 1. allocate node
	Node* new_node = new Node();
	
	// Used in step 5
	Node *last = *head_ref;
	
	// 2. Put in the data
	new_node->data = new_data;
	
	// 3. This new node is going to be
	// the last node, so make next of
	// it as NULL
	new_node->next = NULL;
	
	// 4. If the Linked List is empty,
	// then make the new node as head
	if (*head_ref == NULL) {
		*head_ref = new_node;
		return;
	}
	
	// 5. Else traverse till the last node
	while (last->next != NULL) {
		last = last->next;
	}
		// 6. Change the next of last node
	last->next = new_node;
	return;
}


```


# 2. Удаление из списка (перестановка указателей)

- `Delete()` удаляет элемент, на который указывает итератор, возвращая итератор, ссылающийся на следующий за удалённым элемент.
Если итератор указывает на терминатор, то удаления не происходит, и функция принимает значение терминатора списка `Last()`.

```c

Iterator Delete(List* l, Iterator* i)
{
	Iterator res = Last(l);
	if(Equal(i, &res))
		return res;
	res.node = i->node->next;
	res.node->prev = i->node->prev;
	i->prev->next = res.node;
	l->size--;
	free(i->node);
	i->node = 0;
	return res;
}

```

***
# 3. Удаление всего списка

- `Destroy()` уничтожает список, перебирая и удаляя его элементы.

```c

void Destroy(List* l)
{
	struct Item* i = l->head->next;
	while(i!= l->head)
	{
		struct Item* pi - l->head;
		i = i->next;
		free(pi);
	}
	free(l->head);
	l->head = 0;
	l->size = 0;
}

```

> Как и в случае очереди, возможна рекурсивная версия программы уничтожения, но никакого практического эффекта она не даёт.

==на экзамене нужно написать рекурсивную версию программы выше. эта не подойдёт==

### Leet & Geeks 

```c++

class Solution {
public:
    ListNode* removeElements(ListNode* head, int val) {
        if (head == nullptr) {
            return nullptr;
        }
        if (head->val == val) {
            ListNode* ptr = head->next;
            delete head;
            return removeElements(ptr, val);
        }
        head->next = removeElements(head->next, val);
        return head;
    }
};


```

https://www.geeksforgeeks.org/deletion-in-linked-list/


***
# 4. Конкатенация списков



# 5. Удалить медианный элемент списка
завести счетчик, пройти


# 6. Сравнение линейных списков


# 7. Сложить числа, которые лежат в списках

```c++
#include <iostream>
#include <unordered_set>

using namespace std;

// Linked List Node
struct Node {
    int data;
    Node* next;
};

// Function to insert a node at the beginning of a linked list
void push(Node** headRef, int newData) {
    Node* newNode = new Node();
    newNode->data = newData;
    newNode->next = *headRef;
    *headRef = newNode;
}

// Function to calculate the sum of values from two linked lists using a hash table
int calculateSum(Node* list1, Node* list2) {
    unordered_set<int> hashTable;

    // Insert values from the first linked list into the hash table
    while (list1 != nullptr) {
        hashTable.insert(list1->data);
        list1 = list1->next;
    }

    int sum = 0;

    // Calculate the sum of values from the second linked list while checking for duplicates
    while (list2 != nullptr) {
        if (hashTable.find(list2->data) != hashTable.end()) {
            sum += list2->data;
        }
        list2 = list2->next;
    }

    return sum;
}

int main() {
    // Create the first linked list: 1 -> 2 -> 3 -> 4 -> 5
    Node* list1 = nullptr;
    push(&list1, 5);
    push(&list1, 4);
    push(&list1, 3);
    push(&list1, 2);
    push(&list1, 1);

    // Create the second linked list: 3 -> 4 -> 5 -> 6 -> 7
    Node* list2 = nullptr;
    push(&list2, 7);
    push(&list2, 6);
    push(&list2, 5);
    push(&list2, 4);
    push(&list2, 3);

    // Calculate the sum of values from both linked lists
    int sum = calculateSum(list1, list2);

    cout << "Sum of common values: " << sum << endl;

    return 0;
}

```

# 8. Вычитание чисел на списках

```c++


```



# 9. Реверс списка

```c++
struct Node {
    int data;
    struct Node* next;
    Node(int data)
    {
        this->data = data;
        next = NULL;
    }
};

struct LinkedList {
    Node* head;
    LinkedList() { head = NULL; }
 
    /* Function to reverse the linked list */
    void reverse()
    {
        // Initialize current, previous and next pointers
        Node* current = head;
        Node *prev = NULL, *next = NULL;
 
        while (current != NULL) {
            // Store next
            next = current->next;
            // Reverse current node's pointer
            current->next = prev;
            // Move pointers one position ahead.
            prev = current;
            current = next;
        }
        head = prev;
    }

```




```c++


class Solution {
 public:
  ListNode* reverseList(ListNode* head) {
    if (!head || !head->next)
      return head;

    ListNode* newHead = reverseList(head->next);
    head->next->next = head;
    head->next = nullptr;
    return newHead;
  }
};

```



# 10. Найти длину списка

```c++
Follow the given steps to solve the problem:

Initialize count as 0 
Initialize a node pointer, current = head.
Do following while current is not NULL
current = current -> next
Increment count by 1.
Return count 

/* Link list node */
class Node {
public:
    int data;
    Node* next;
};
 
/* Given a reference (pointer to pointer) to the head
of a list and an int, push a new node on the front
of the list. */
void push(Node** head_ref, int new_data)
{
    /* allocate node */
    Node* new_node = new Node();
 
    /* put in the data */
    new_node->data = new_data;
 
    /* link the old list of the new node */
    new_node->next = (*head_ref);
 
    /* move the head to point to the new node */
    (*head_ref) = new_node;
}
 
/* Counts no. of nodes in linked list */
int getCount(Node* head)
{
    int count = 0; // Initialize count
    Node* current = head; // Initialize current
    while (current != NULL) {
        count++;
        current = current->next;
    }
    return count;
}


```

