# short plan



Функция checkStr анализирует массив символов (ptr) для выявления подстрок, начинающихся с заглавных букв. Затем она записывает результаты анализа в файл (file) и сохраняет сообщение об ошибке в сегменте общей памяти (shared_error).

Давайте разберем эту функцию:

    std::string res;: Эта строка инициализирует пустую строку res, которая будет использоваться для накопления сообщений об ошибках.

    Функция выполняет итерацию по массиву символов ptr от индекса 1 до размера - 2 (исключая первый и последний символы). В этом цикле каждый символ проверяется на то, является ли он заглавной буквой и заканчивается ли подстрока, начинающаяся с этого символа, символами '\0' или '\n'.

    Если по индексу i найдена заглавная буква, запускается другой цикл для извлечения подстроки, начинающейся с этого индекса, пока не будет достигнут конец строки или не встретится символ новой строки. Извлеченная подстрока сохраняется во временной строке tmp.

    Затем извлеченная подстрока tmp записывается в файл с помощью fprintf, добавляя символ новой строки.

    Сообщение об ошибке, указывающее на то, что строка начинается с прописной буквы, добавляется к результирующей строке res.

    Индекс i внешнего цикла обновляется до индекса, на котором заканчивается подстрока (j), пропуская символы, обработанные во внутреннем цикле.

    Если символ в индексе i не является прописной буквой, то просто пропускается текущая подстрока, обновляя индекс i до конца подстроки.

    В обоих случаях выдается сообщение об ошибке, указывающее на то, что






Эта функция `getInput` считывает символы со стандартного ввода (stdin) до тех пор, пока не будет встречен конец файла (EOF), динамически выделяя память для хранения входных символов в виде строки в стиле C. Давайте разберем эту функцию:

1. `char* getInput(int& size) {`: Эта функция принимает в качестве параметра целочисленную ссылку `ize`, которая будет хранить размер входной строки.

2. `char symbol;`: В этой строке объявляется переменная `symbol` типа `char`, в которой будет храниться каждый символ, считанный из stdin.

3. `char* in = (char*)malloc(sizeof(char));`: Эта строка инициализирует указатель `in` на динамически выделяемый блок памяти размером 1 байт (достаточный для хранения нулевого терминатора).

4. `if (in == NULL) { perror("malloc"); }`: Эта строка проверяет, было ли выделение памяти успешным. Если `malloc` возвращает `NULL`, это указывает на ошибку, а `perror` печатает сообщение об ошибке.

5. `size_t i = 0; size = 1;`: Эта строка инициализирует счетчик `i` в 0 и устанавливает размер входной строки `ize` в 1.

6. `std::cout << "Введите какую-нибудь строку. Если вы хотите остановиться, нажмите Ctrl+D" << std::endl;`: Эта строка выводит сообщение, предлагающее пользователю ввести строки, и сообщает о необходимости остановиться, нажав Ctrl+D.

7. `while ((symbol = getchar()) != EOF) {`: Этот цикл считывает символы из stdin до тех пор, пока не будет встречен EOF. Внутри цикла:

   - `in[++i] = symbol;`: Эта строка сохраняет символ `символ` по индексу `i+1` в динамически выделяемом блоке памяти. Перед обращением к массиву `++i` увеличивает `i`,












## Вариант 20



- Освоение принципов работы с файловыми системами  
- Обеспечение обмена данных между процессами посредством технологии «File mapping»

Составить и отладить программу на языке Си, осуществляющую работу с процессами и  
взаимодействие между ними в одной из двух операционных систем. 

- В результате работы программа (основной процесс) должен создать для решение задачи один или несколько дочерних процессов. 
- Взаимодействие между процессами осуществляется через системные  сигналы/события и/или через отображаемые файлы (memory-mapped files).  
- Необходимо обрабатывать системные ошибки, которые могут возникнуть в результате работы.





***
## Memory mapping

ASIDE: M M A P() AND PERSISTENT MEMORY


Memory mapping is an alternative way to access persistent data in files. The mmap() system call creates a correspondence between byte offsets in a file and virtual addresses in the calling process; the former is called the backing file and the latter its in-memory image. The process can then access the backing file using CPU instructions (i.e., loads and stores) to the in-memory image.

By combining the persistence of files with the access semantics of memory, file-backed memory mappings support a software abstraction called persistent memory. The persistent memory style of programming can streamline applications by eliminating translation between different data formats for memory and storage

```c++
p = mmap(NULL, file_size, PROT_READ|PROT_WRITE, 2 MAP_SHARED, fd, 0); 
assert(p != MAP_FAILED); 
for (int i = 1; i < argc; i++) 
	if (strcmp(argv[i], "pop") == 0) // pop 6 
		if (p->n > 0) // stack not empty 
			printf("%d\n", p->stack[--p->n]); 
			} 
			else { // push 
				if (sizeof(pstack_t) + (1 + p->n) * sizeof(int) 10 <= file_size) // stack not full 11 p->stack[p->n++] = atoi(argv[i]); 12 }
```

The program pstack.c (included on the OSTEP code github repo, with a snippet shown above) stores a persistent stack in file ps.img, which begins life as a bag of zeros, e.g., created on the command line via the truncate or dd utility. The file contains a count of the size of the stack and an array of integers holding stack contents. After mmap()-ing the backing file we can access the stack using C pointers to the in-memory image, e.g., p->n accesses the number of items on the stack, and p->stack the array of integers. Because the stack is persistent, data push’d by one invocation of pstack can be pop’d by the next.

hard