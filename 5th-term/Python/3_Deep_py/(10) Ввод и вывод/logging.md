
- logging

```
def get_logger(name: str, log_level: str = 'INFO') -> logging.Logger:
# what is that "name : str", etc?


what is that func definition?
is it a good idea to write docstrings?


Format:
	Args:
		Format : пояснение
	Raises:
		Exception (or other standard type): Если....
	Returns:
		Format : пояснение


> understand how lists are behave as args into func definitions.
> work with jsons as with pointers (in what case? checkout python libs!)

- function annotations?


```
