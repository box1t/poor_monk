

1. В папке проекта в терминале IDE ввести:

```sh
python3 -m venv .venv
source .venv/bin/activate
# deactivate
```

2. Сохранить / подгрузить библиотеки:

```sh
pip freeze > requirements.txt
pip install -r requirements.txt
```


