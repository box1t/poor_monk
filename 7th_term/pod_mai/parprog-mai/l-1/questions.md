1 ЛР
1. Виды памяти?
- Кеш-память
- Разделяемая память
- Текстурная память - возможность кеширования данных в двухмерном измерении

- Реrистры - Наиболее простым вид. Имеющиеся реrистры распределяются между нитями блока на этапе компи
ляции (и, соответственно, влияют на количество блоков, которые может выпол
нять один му льтипроцессор ).

Каждая нить получает в свое монопольное пользование некоторое количество
реrистров, которые доступны как на чтение, так и на запись (readjwrite). Нить не
имеет доступа к реrистрам друrих нитей, но свои реrистры доступны ей на протя
жении выполнения данноrо ядра.
Поскольку реrистры расположены непосредственно в потоков ом мультипро
цессоре, то они обладают максимальной скоростью доступа.


- Локальная - размещена в DRAM.
Применяется для размещения локальных данных, если имеющихся регистров не хватает.
Доступ к ней характеризуется высокой латентностью - от 400 до 600 тактов.

- Разделяемая 

расположена непосредственно в потоковом мульти
процессоре, но она выделяется на уровне блоков  каждый блок получает в Boe
распоряжение одно и то же количество разделяемой памяти. Bcero каждый муль
типроцессор содержит 16 Кбайт разделяемой памяти, и от Toro, сколько разделяе
мой памяти требуется блоку, зависит количество блоков, которое может быть за
пущено на одном мультипроцессоре.

Обладает небольшой латентностью.


- rлобальная - расположена вне gpu, это обычная DRAM-память, выделяемая на CPU. все нити могут читать и писать в глобальную память.
- Константная - при помощи модификатора constant
- Текстурная


- Линейная память - память, существующая на gpu (гл.3 Боресков)
- Блок-линейная память - непрозрачный контейнер, управление которым осуществляет драйвер

2. Самая быстрая память? Вторая по скорости?
- регистровая - они расположены в потоковом мультипроцессоре и обладают максимальной скоростью доступа.
- разделяемая. второе место, тк выделяется на уровне блоков - одно и то же кол-во разделяемой памяти.

3. Какие виды памяти располагаются на мультипроцессоре?
- регистровая
- разделяемая

4. В какой памяти находится локальная переменная в ядре? А аргументы ядра?
5. Можно ли разыменовывать указатель на память GPU с CPU? Работает ли адресная
арифметика для указателей на память GPU на CPU?
6. Что такое warp?

- нити на GPU обладают крайне небольшой стоимостью создания, управле
ния и уничтожения (контекст нити минимален, все реrистры распределе
ны заранее);
О для эффективной заrрузки GPU необходимо использовать MHoro тысяч
отдельных нитей, в то время как дЛЯ CPU обычно достаточно 1020 нитей.


Важным моментом является то, что' хотя подобный подход очень похож на pa
боту с SIМDмоделью, есть и принципиальные отличия (компания Nvidia исполь
зует термин SIMT  Siпgle Iпstructioп, Multiple Thread). Нити разбиваются на rруп
пы по 32 нити, называемые wаrp'ами. Только нити в пределах одноrо warp'a
выполняются физически одновременно. Нити из разных warp'oB MorYT нахоДИТЬ
ея на разных стадиях выполнения проrраммы. При этом управление wаrp'ми про
зрачно осуществляет сам GPU.
ДЛЯ решения задач CUDA использует очень большое количество параллель
Но выполняемых нитей, при этом обычно каждой нити соответствует один эле
мент вычисляемых данных. Все запущенные на выполнение нити орrанизованы
в следующую иерархию (рис. 2.1).
Верхний уровень иерархии  сетка (grid)  соответствует всем нитям, выпол
IiЯЮщим данное ядро. Верхний уровень представляет из себя одномерный или
двухмерный массив блоков (block). Каждый блок  это одномерный, двухмерный



или трехмерный массив нитей (thread). При этом все блоки, образующие сетку,
имеют одинаковую размерность и размер.
Каждый блок в сетке имеет свой адрес, состоящий из одноrо или двух неотри
цательных целых чисел (индекс блока в сетке). Аналоrично каждая нить внутри
блока также имеет свой адрес  одно, два или три неотрицательных целых числа,
задающих индекс нити внутри блока.

- Подобное разделение всех нитей является еще одним общим приемом исполь
зования CUDA  исходная задача разбивается на набор отдельных подзадач, pe
шаемых независимо друr от друrа (рис. 2.2). Каждой такой подзадаче соответ'"
ствует свой блок нитей.
При этом каждая подзадача совместно решается всеми нитями cBoero блока,
Разбиение нитей на warp'bI происходит отдельно для каждоrо блока; таким обра'"
зом, все нити одноrо warp'a всеrда принадлежат одному блоку. При этом нити MO
rYT взаимодействовать между собой только в пределах блока. Нити разных бло'"
ков взаимодействовать между собой не MorYT.



7. Что будет если часть потоков warp’а пойдут в одну ветку if, а часть в другую?
8. Могут ли warp’ы одного блока работать на разных мультипроцессорах?
9. Сколько физически потоков будет при запуске ядра с параметрами <<<1, 1>>>?
10. Что общего между кеш-памятью и разделяемой памятью?
11. Что возвращают все функции CUDA API?
12. Модификаторы __device__, __host__, __global__?

CUDA строится на концепции, что GPU (называемый устройством, device)
выступает в роли массивнопараллельноrо сопроцессора к CPU (называемому
host). Проrрамма на CUDA задействует как CPU, так и GPU. При этом обычный
(последовательный, то есть непараллельный) код выполняется на CPU, а для
массивнопараллельных вычислений соответствующий код выполняется на GPU
как набор одновременно выполняющихся нитей (потоков, threads).

- В приведенном выше листинrе первая функция (sumKernel) является ядром 
она будет параллельно выполняться для каждоrо набора элементов а [ i ] , ь [ i] и
c[i]. Спецификатор gobal используется для обозначения Toro, что это
ядро, то есть функция, которая работает на GPU и которая может быть вызвана
(точнее, запущена сразу на большом количестве нитей) только с CPU.



Спецификаторы host и device MorYT быть использованы вместе
(это значит, что соответствующая функция может выполняться как на GPU, так и
на CPU  соответствующий код для обеих платформ будет автоматически creHe
рирован компилятором). Спецификаторы global и host не MoryT быть
использованы вместе.
Спецификатор global обозначает ядро, и соответствующая функция
должна возвращать значение типа void.
На функции, выполняемые на GPU (device и global), накладыва
ются следующие оrраничения:
О нельзя брать их адрес (за исключением global функций);
О не поддерживается рекурсия;
О не поддерживаются stаtiспеременные внутри функции;
О не поддерживается переменное число входных aprYMeHToB.

