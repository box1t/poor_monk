1. Каков состав custom класса linreg?
	- fit
	- predict
для каких задач этого набора достаточно ? 
Почему в рамках ЛР было реализовано больше методов?
Как с ходу применить fit и predict? 
Как учесть датасет при применении кастомного класса с ходу?
 
2. Что входит в состав init?
Зачем нужен lr? Почему стандартное значение 0.001?
Почему n_iters=1000?
Что происходит с ростом, с уменьшением lr, n_iters?

3. А что в составе sklearn?


4. Почему weights в составе init, а не как массив? Аналогичный вопрос к bias.



5. Почему линейную регрессию называют градиентным спуском?
Я правильно понимаю, что есть МНК-линрег и градиентный спуск (2 версии)?

6. Почему в строчке         n_samples, n_features = X.shape
2-ум переменным присвоена одна shape?
За что отвечает shape?

Почему весам присваивают заполненный нулями features?
self.weights = np.zeros(n_features)
Верно ли понимаю, что при такой инициализации изначально пустой features заполнится нулями?
Или то, что содержало значения X.shape, станет нулями?
X.shape в данном случае это какой атрибут?
атрибут списка?

7. 
