

```
Почему бы не решать классификацию как задачу регрессии? (а откуда такое желание?)  

Почему данные представляются как точки на прямой в плоскости?
Как нужно менять расстояние между прямой и признаками?
Но разве задача классификация это задача приближения прямой к точкам?

Зачем так рассматривать?  
Каким способом свести задачу регрессии к классификации?  
Как приближать прямую к признакам? Как делить числа?

Что если навесить функцию знака к функции?  
Что если скалярное произведение больше или меньше нуля? А если ноль? Значит модель не знает, к какому классу точно отнести.

Как классифицировать тот или иной объект, особенно если возник ноль при вычислении предсказания класса по объекту?

Что нам даёт разделяющая прямая и как подвязать её к объектам? Уверенность модели как скалярное произведение говорит, принадлежит ли модель к этому классу или нет?

И какого класса будет модель?

Вот если будет такая функция, функция знака разрывная. Как её оптимизировать? Можно взять и записать это дело в следующем виде.

Мы хотим построить функцию ошибки, уменьшающую число ошибок на выборке. Для этого используем запись

Мы хотим минимизировать число несовпадений классов.

Квадратные скобки вычисляют логическое выражение. Если классы не Совпали, она возвращает 1.  
Скобки айверсона.  
Удобно можно запрограммировать.  
(сумма логических выражений как функция ошибки?)

Классы у нас разные, один либо положительный либо отрицательный, и в целом будет отрицательно.

Что характеризует отступ? Misclassification loss.  
И эта штука при умножении на таргет показывает, насколько модель уверенна.

Допустим есть 2 модели. У одной отступ большой, а другая меньше уверена чем другая.

Какая из моделей лучше?  
Подойдёт та, которая не совсем уверенна.  
Почему?

Как оптимизировать ступенчатую функцию? У нас левая и правая производные разные, это не дифференцируемо?

Функция с устранимым разрывом (гипербола неустранима) - точка скачка.

Мажорируем функцию!

Лосс для перцептрона.

Производная для графиков, какое значение если прямая 0, 0 + b.

Розенблат классифицирова несколько объектов. Svm , hurge loss.



Перцептрон и разделяющие прямые. Вошьмем прямую, которая бы отсупала на одинаковом расстоянии от выборок.  
Вступает в дело метод опорных векторов Svm. Мы не просто ращбелчпм, но и находим прямую, разделяющую 2 класса. При этом ...(условие).

Задача классификации это задача построения гиперплоскости для разделения данных.

Как идея - использовать вероятности. Уверенность модели - звучит не очень интерпретируемо. Поэтому для сведения задачи к вероятности используем логистическую регрессию.

Всё ещё смотрим бинарную классификацию. Решаем задачу лин регрессии.

Берем логарифм от вероятности положительного к отрицательному исходу, и приравниваем.  
(свели лин регрессию к отрезку 0,1).

Есть функция сигмоиды.

Применим сигмоиду к скалярному произведению.  
Почему сигмоида? Преобразование сильно похоже на неё.

Почему бы не воспользоваться методом макс правдоподобия, функцией правдоподобия, или методом моментов?

Насколько вероятно получить это значение при условии заданных параметров?

Можно использовать понимание, что есть два исхода.  
Можем использовать схему бернулли. Тогда перепишем эту функцию, учитывая схему.

Но мы хотим эту штуку использовать как лосс.

Так как много множителей, будем логарифмировать, а не дифференцировать из за рекурсии.

Но это не готово, так как логарифмирование привело к логарифмам. Логарифм существует не везде. Придётся работать с градиентами.

Почему после выведения лоссов получаем логистическую не классификацию а регрессию?  
Потому что в результате получаем число.

Группировка классов один против всех. Один класс отличается от всех.  
Но тут не спасёт нормализация признаков.

Второй вариант это все против всех - разбить на пары.  
Попарно делим каждый класс, учим отличать I от j.

Но порядок k^2.  
Сложновато, тк есть более простые методы.

Выбор голосованием argmax.

Записывается как 2 знака сумм.

Как свести лог регрессию к многоклассовой классификации?

(перевести числа в отрезок 0,1)  
Используем функцию софт макс.  
(выведи с ручкой)

Запишем функцию правдоподобия через софт макс.  
А затем прологарифмируем.

/раскрутить устно идею с логарифмам и экспонентами - устно решай задачи зорич.

У нас метод Максимального правдоподобия. Наиболее вероятного предсказания.


```

