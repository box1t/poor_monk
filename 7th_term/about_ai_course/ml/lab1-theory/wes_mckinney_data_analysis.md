
## Chap_5

```
1. Объём понятия "структурированные данные"?
	- табличные данные
	- матрицы / многомерные массивы
	- отношения с внешним и первичным ключом (почему это не одно и то же, что табличные данные?)
		- несколько таблиц, связанных между собой по ключевым столбцам.
	- временные ряды

2. Перечисли основные объекты Pandas и их назначение
	- DataFrame - двумерная таблица, где строки и столбцы размечены
		- выбрана к созданию по аналогии с data.frame в языке R.
	- Series - одномерный размеченный массив 

3. Подскажи, как в Pandas выполнять эти действия: 
- индексирование, позволяющее без труда изменять форму наборов данных, 
- формировать продольные и поперечные срезы, 
- выполнять агрегирование 
- выбирать подмножества.

4. Расшифруй акроним pandas
	- panel data (панельные данные) - эконометрический термин.
	- python data analysis


5. Опиши содержимое Series.
	- массив данных одного типа
	- и массив меток - называется индексом
6. Где находится индекс Series?
	в строковом представлении (интерактивный режим iPython):
	- индекс слева, а массив справа
	
7. Опиши DataFrame
	- DataFrame представляет табличную структуру данных, состоящую из
	упорядоченной коллекции столбцов, причем типы значений в разных столбцах могут различаться.

8. Перечисли ходовые методы работы с DataFrame
	- frame.head() - первые 5 строк
	- frame.tail() - последние 5 строк
	- pd.DataFrame(data, columns=["year", "state", "pop"]) - строгое задание последовательности столбцов
	- при обращении к столбцу, которого нет в data, он заполнится значениями NaN.
	- frame2["state"] - извлечь DataFrame как объект Series
	- frame2.year - получить доступ к столбцам DataFrame как к атрибутам 
	- iloc и loc - для извлечения строк по позиции или по имени
	- модификация столбца путём присваивания.

```

## Chap_9


```
### Рисунки и подграфики
1. Как создать график в matplotlib?
	- вызвать объект figure() этой библиотеки (		plott = plt.figure()	)
	- какие параметры figure()?
		- figsize
	- какие ограничения при создании графика?
		- add_subplot(x, y, 1), x*y, numeration_start=1
	- plt.subplots - подробнее.
	- твои графики в лабе ЧМ и в каггле

### Задание свободного места вокруг подграфиков



### Цвета, маркеры и стили линий

Функция рисования линии – plot – принимает массивы координат x и y, а также
необязательные параметры стилизации. Например, чтобы нарисовать график
зависимости y от x зеленой штриховой линией, нужно написать:

- ax.plot(x, y, linestyle="--", color="green")

- любой цвет можно представить своим шестнадцатеричным кодом
(например, '#CECECE').

- в IPython или Jupyter введите plot? - цвета перечислены в строке документации для функции plot


### Риски, метки и надписи

- Добавление пояснительных надписей - label, legend



### Аннотации и рисование в подграфике



### Сохранение графиков в файле



### Конфигурирование matplotlib

### Построение графиков с помощью pandas и seaborn


	

```










