
```

стр. 50 - 59
1. Почему KNN один из простейших алгоритмов МО?
2. Что следует из запоминания KNN-ом обучающего набора данных? 
3. Насколько другие алгоритмы МО запоминают обучающую выборку?
4. Насколько запоминание обучающей выборки противоречит переобучению?
5. Что значит "найти ближайших соседей"?
6. На что влияет число соседей? Когда выгодно рассматривать 1, 3, 5 соседей?
7. Как связаны сосед и точка, в которой желаем получить прогноз?
8. Если k = 1, значит ли это, что и прогноз будет всего один? Насколько рис. 2.4 подтверждает или опровергает гипотезу?
9. Если точек данных несколько, прогнозов тоже несколько при k = 1?
10. Что является прогнозом для точки данных, связанной с точкой обучающего набора?
11. Какие точки сопоставляет алгоритм KNN при выполнении предсказания?
12. Каков принцип голосования в методе KNN при K > 1?
13. Почему подсчет происходит для точки тестового набора, а не обучающего?
14. Какой класс называют частовстречающимся в KNN? 
	- набрал больше всего голосов. каких? среди k ближайших соседей.
	- а что является ближайшим соседом? класс 0 или 1? согласно которому подходит или не подходит?

15. Чем KNN отличается от бинарной классификации?
16. Утверждается, что KNN применим для более чем 2 классов. Что из этого следует?
	- подсчитываем кол-во соседей, принадлежащих к каждому классу, и снова прогнозируем наиболее часто встречающийся класс
	
----
17. Что следует из разделения набора данных на обучающую и тестовую выборки?
	- узнаем обобщающую способность модели.
	- какова роль этой способности для выполнения прогнозов?
		- способность рассмотрена в главе 1. со стр. 52. но это просто КОД.
		- т.е. X(train/test), Y(train/test) помогают оценить обобщ. способность модели.



18. Что происходит после разделения датасета на обучающую и тестовую выборки?
	- создаем объект классификатора из библиотеки sklearn, задаем параметр числа соседей = 3.
	- затем выполняем подгонку (fit), используя обучающий набор.
- а как бы тут участвовал тестовый набор? или он уже участвует при разделении ?
- чем отличается подгонка от получения прогноза?
	- подгонка (fit) выполняется на обучающей выборке.
	- получение прогноза (predict) выполняется на тестовой выборке.
- верно ли, что эти операции одно и то же, разница в датасете (по назначению, по датасету)?
	- fit использует (clf.fit(X_train, y_train))
	- predict использует (clf.predict(X_test))

19. Как после fit, predict оценить обучающую способность модели?
	- вызвать метод score для X_test, y_test)

20. Как меняется качество прогнозов с увеличением числа соседей?
	- сглаживается граница принятия решения.
	- с ростом числа соседей модель упрощается, граница принятия решения становится более гладкой.
	- сложная модель при малом числе соседей.


21. Определите границу принятия решения в алгоритме KNN.


22. Охарактеризуйте границу принятия решения, хорошо согласованную с обучающими данными.

23. Опишите случай равенства числа соседей и точек данных обучающего набора.

24. Как (по какой системе) связаны сложность модели и обобщающая способность?



```
