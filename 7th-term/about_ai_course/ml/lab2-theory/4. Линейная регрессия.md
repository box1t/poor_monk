

- [линейная регрессия хендбук](https://education.yandex.ru/handbook/ml/article/linear-models)
- [линейная регресиия мгу](https://deepmachinelearning.ru/docs/Machine-learning/Linear-regression-extensions/Linear-regression)

1. Линейная регрессия. Постановка задачи, определение модели, формула модели линейной регрессии. Свойства линейной регрессии. Особенности линейной регрессии и применение на практике.


### **Linear Regression**

- геометрический смысл линейной регрессии?
	- регрессия - от слова регрессия к среднему.
	- геометрически это прямая, которая стремится быть на минимальном расстоянии от каждой точки выборки, от ошибки каждой точки выборки?
	- линейная - ?
	- а геометрический смысл ошибок в линейной регрессии? площадь квадратов со стороной длины ошибки. и что это значит? в чем смысл этого квадрата? в чем смысл этих ошибок? откуда они возникают? при решении решении системы, построенной на основе всех признаков линейной функции. а почему именно систему решаем? а на основе чего система? ошибки. ошибки чего? ошибка это разность, которая закладывается в виде разности y и суммы коэф * признак
	- сводится линейная регрессия к задаче оптимизации MSE?


- в чем заключается МНК? как его вывести? 
- каковы сферы применения МНК в мл и какие его преимущества? как связан с переобучением? с обучением? с данными? с выбросами? с градиентом? с градиентным спуском? с разными видами градиентного спуска?


- что такое вклад в контексте лин рег?

- отличие временных рядов от регрессии?
- к чему регрессируем?


- связь регуляризации и кросс-валидации.
	- коэф регуляризации подбирается на кросс-валидации

- нужно ли нормализовывать таргет - предсказываемую переменную?
	- В регрессии нормализация таргета может ускорить сходимость, если его значения сильно разнятся.
	- но все таки нормализация это про норму весов.
	- В классификации нормализация таргета не нужна.

- что отвечает за важность признаков в лин рег?

- почему гребневая регрессия помогает с вырожденностью матрицы грама XtX? 



```

- каков геометрический смысл решения задачи линейной регрессии?
- А что будет значить линейность для задачи регрессии, классификации?
- можно ли на вход линейной регрессии подать категориальные признаки? как?
	- закодировать one-hot-encoding
- Расскажи о достоинствах и недостатках линейной регрессии
- Будет ли изменение масштаба признаков (после перенастройки модели с новым масштабом) влиять на прогнозы линейной регрессии?

- Как аналитически решается задача линейной регрессии?

- Задача: решаем задачу линейной регрессии, все y > 0, какие алгоритмы из написанных могут дать отрицательное значение: линейная регрессия, KNN, градиентный бустинг, дерево, случайный лес, нейросеть?


```

## 1. Линейная регрессия. Постановка задачи. 

**Задача линейной регрессии** заключается в предсказании вещественного значения целевой переменной \( y \) для размеченных данных на основе набора признаков \( x_1, x_2, ..., x_n \). 

## 2. Определение модели, формула модели линейной регрессии.

Линейные методы предполагают, что между признаками объекта (features) и целевой переменной (target) существует линейная зависимость, то есть

$$y = w_1 x_1 + w_2 x_2 + ... + w_k x_k + b, $$
где $у$ - целевая переменная (что мы хотим предсказать), $x_i$ - признак объекта $х$, $w_i$ - вес $i$-го признака, $b$ - bias (смещение, свободный член)

- Модель линейна по параметрам, но не обязана быть линейной по признакам.

- Все признаки отражают значения некоторых счётчиков, которые коррелируют с целевой переменной.

- Предсказанные значения y используются для вычисления параметров w модели, которые минимизируют эмпирический риск.

- Риск — это **среднее количество ошибок, допускаемых моделью на данных.
	- эмпирический - Это **ошибка модели на тренировочных данных**.
		- Вычисляется как усреднённое значение функции потерь на обучающей выборке


- Часто предполагают, что объект $х$ содержит в себе фиктивный признак равный 1 для представления свободного члена $b$. В этом случае формула принимает простой вид:

$$y = \langle w, x \rangle,$$

где $\langle \cdot, \cdot \rangle$ - скалярное произведение векторов $w, x \in \mathbb{R}^n$.

  
В матричной форме, в случае, когда у нас есть $m$ объектов формулу можно переписать следующим образом:
$$ Y = Xw, $$ где $Y$ - столбец размера $n$, $X$ - матрица признаков размера $n \times k$ (каждая строка матрицы есть описание признаков объекта), $w$ - вектор весов размера $k$.

**Лосс:**

$$

\begin{aligned}

L(y_{pred}, Y) &= \frac{1}{n}||y_{pred} - Y||^2_2 = \frac{1}{n}||Xw - Y||^2_2 = \frac{1}{n}\sum_{i=1}^{n}\left(\sum_{j=1}^{m} x_{ij}w_j - y_i\right)^2 =: L(w)

\end{aligned}

$$




## 3. Свойства линейной регрессии.


- Достоинства линейной регрессии
	- интерпретируемость
	- наличие аналитического решения при минимизации суммы квадратов прогнозов как глобального минимума, т.е. лучшим значением из всех возможных (Конвексность оптимизационной задачи: - смотри биро и тетрадь)
		- Этот критерий по ww является выпуклым как суперпозиция линейной и выпуклой функции (веса находятся по принципу минимизации наименьших квадратов)
	- Решение оптимизационной задачи сводится к нахождению матрицы весов:
	- **Чувствительность к мультиколлинеарности:**
	- Ошибки (ϵ\epsilonϵ) модели предполагаются нормально распределенными с нулевым средним:
	- **Гипотеза гомоскедастичности:** (приплети bias-variance)
		- **Невлияние шумовых признаков:** 
	- Чувствительность к выбросам
		- так как минимизация функции ошибки сильно зависит от отклонений в данных.
	- Коэффициенты зависят от масштаба признаков. Для адекватной интерпретации и стабильности модели данные обычно нормализуют или стандартизируют. 
	- Линейную регрессию можно улучшить, добавив регуляризационные члены (L1, L2):
	- Если зависимость между признаками и целевой переменной нелинейная, модель будет иметь высокое смещение (недообучение).
	- изменение масштаба признаков не влияет на прогнозы для лин рег без регуляризации, а с регуляризацией - влияет.

### **3. Свойства шума ϵ\epsilonϵ:**

#### **Свойство 1: E[ϵ]=0\mathbb{E}[\epsilon] = 0E[ϵ]=0**

- Ожидаемое значение шума равно нулю.
- Это означает, что шум распределён симметрично относительно истинного значения f(x)f(x)f(x) и в среднем не приводит к смещению предсказаний.

#### **Свойство 2: Var[ϵ]=E[ϵ2]=σ2\text{Var}[\epsilon] = \mathbb{E}[\epsilon^2] = \sigma^2Var[ϵ]=E[ϵ2]=σ2**

- Дисперсия шума постоянна (гомоскедастичность).
- σ2\sigma^2σ2 — дисперсия ошибки, показывающая разброс значений шума вокруг нуля.
- Важно, чтобы дисперсия шума была одинаковой для всех наблюдений. Если она различается, возникает эффект гетероскедастичности, что усложняет обучение и интерпретацию модели.

---

### **Интерпретация этих свойств**

- Эти свойства являются **основными предположениями линейной регрессии**.
- Если они нарушаются (например, E[ϵ]≠0\mathbb{E}[\epsilon] \neq 0E[ϵ]=0 или дисперсия ϵ\epsilonϵ не постоянна), модель может быть некорректной или результаты не будут статистически значимыми.
- В реальной жизни шум не всегда удовлетворяет этим предположениям, поэтому применяются методы вроде регуляризации или преобразования данных.




- геометрическая интерпретация линейной регрессии (общий вид)
	- минимизация квадратов расстояния между точками выборки и плоскости модели
	- прямая, лежащая на минимальном расстоянии от каждой из точек выборки
	- поиск прямой, которая проходит через все точки, лежащие на плоскоси (либо проходит рядом с прямой)


- как линейная модель масштабируется на сложные задачи?
	- с помощью усложнения признаков - feature engineering. 
	- при этом может хромать интерпретируемость.


- Решить уравнение можно 
	- МНУ
	- GD

- решение задачи регрессии 
	- поиск прямой, которая проходит через все точки, лежащие на плоскости (либо проходящей рядом прямой).



### **1. Аналитическое решение**

Минимизация ошибки по методу наименьших квадратов дает решение: $$ w = (X^TX)^{-1}X^TY $$
- почему совершаем переход к ошибкам, отклонениям вместо решения системы линейных уравнений?
	- система вряд ли решается точно
	- система мб несовместна 

### **2. Численное решение**

Используются градиентные методы.





## 4. (Проблемы) Особенности линейной регрессии

### **1. Проблема вырожденности матрицы**

В формуле для весов (параметров) линейной регрессии
производится обращение матрицы, которая может оказаться вырожденной.

Решения этой проблемы:
- регуляризация,
- селекция (отбор) признаков,
- уменьшение размерности (в том числе, PCA),
- увеличение объёма выборки.

Матрица (Xт X)(-1) Xт называется псевдообратной матрицей
Мура-Пенроуза (это обобщение обратной на неквадратные матрицы).


#### **Решение 1**


- почему гребневая регрессия помогает в борьбе с вырожденностью матрицы (XT X)?
	- К неотрицательно определённой матрице Грама X т X мы при  lambda > 0 прибавляем положительно определённую диагональную матрицу Грама. В итоге получается заведомо невырожденная матрица (т.к. она положительно определена).

![](Pasted%20image%2020250107065044.png)

- как меняется наклон прямой в зависимости от коэффициента регуляризации?
	- при больших коэффициентах параметр, отвечающий за наклон прямой, становится близким к нулю.
	- наше решение практически превращается в константное (оно соответствует среднему целевому значению). Это вполне логично, при lambda -> +inf мы «заставляем» все коэффициенты занулиться, кроме свободного члена w0 , и наше решение превращается в константное.

- как решение (получаемые коэффициенты) зависят от коэффициента регуляризации в l2?
	- При увеличении коэффициента параметры линейной регрессии устремляются к нулю, но не зануляются.

- как l1 регуляризатор влияет на решение?
	- зануляет коэффициенты.



#### **Решение 2**


Кратко поговорим про второе решение проблемы вырожденности матрицы
X т X – селекцию (отбор) признаков (подробнее см. в соответствующей
главе). Необходимость отбора можно проиллюстрировать примером линейно
зависимых признаков, из-за которых наша матрица вырождена, если убрать
лишние зависимости, оставив в матрице X только линейно-независимые
столбцы, то при m  n  1 матрица X т X станет невырожденной.
Заметим, что в методе LASSO автоматически произошёл отбор признаков:
зануление весов означает, что решение не зависит от соответствующих
признаков. Дальше мы увидим, что автоматический отбор признаков бывает и в
других моделях (не только линейных).

#### **Решение 3**

Третье решение проблемы вырожденности матрицы X т X – уменьшение
(сокращение) размерности признакового пространства (это тоже тема
отдельной главы). Пока приведём иллюстрацию, что такое сокращение
размерности и чем это отличается от селекции признаков, см. рис. XX.11 – при
сокращении размерности мы получаем новое признаковое пространство с
меньшим число признаков, нет гарантии что новые признаки совпадают с


### **2. Проблема мультиколлинеарности**

Этот случай, когда в признаках есть линейные зависимости (здесь мы рассмотрели только равенство признаков). В эконометрике наличие таких зависимостей называется
мультиколлинеарностью.


### **3. Проблема неустойчивости к выбросам**
- классические линейные методы «страдают» из-за выбросов в обучающей
выборке,


В общем случае при решении задачи с весами используют также следующие
приёмы:
1) переходят к новым данным (как было показано выше): признаки и целевые
значения умножают на веса v1 , , vm (отметим, что мы существенно
использовали линейность модели – для других моделей этот приём не пройдёт,
кроме того, неверно говорить, что здесь просто все Не забывайте про фиктивный
признаки умножаются на вес объекта, так в
признак: он часто «ломает»
очевидную интерпретацию
регрессии со свободном членом фиктивный
некоторых
преобразований.
единичный признак также умножится);
2) если v12 , , vm2 целые числа, то можно продублировать объекты: внести
соответствующее число копий каждого объекта в выборку, формально это
приводит к эмпирическому риску (XX.6);
3) использовать стохастический градиентный спуск (SGD) со специальными
вероятностями выбора объектов. Это более универсальная техника, подходит
для алгоритмов, которые обучаются с помощью SGD, а также для алгоритмов
при обучении которых происходит сэмплирование объектов (взятие
подвыборок). Например, в методе SGD i -й объект выбирается с вероятностью
- для решения этой проблемы можно строить робастную регрессию,



## 5. Применение на практике.

### примеры
> применение на практике это вопрос 2!!!!




Оба предположения линейной регрессии на практике, скорее всего не верны: признаки влияют скорее всего нелинейно и характер этого влияния зависит от значений других признаков. Например, при оценке стоимости квартиры по её характеристикам, стоимость нелинейным образом зависит при увеличении расстояния до метро, причем характер этого влияния разный в зависимости от того, есть ли рядом остановка общественного транспорта или нет.

- ну и для неё важно готовить данные.

- а далее примеры - скоринг, etc.


Использовать линейную регрессию имеет смысл, если:

- Зависимость между признаками и целевой переменной близка к линейной.
- Данные содержат небольшое количество признаков.
- Требуется простая и интерпретируемая модель.



### **лин рег в sklearn (на практике)**
`LinearRegression` из библиотеки `scikit-learn` - это один из наиболее распространенных (готовых) алгоритмов для решения задач линейной регрессии.

После обучения модели `LinearRegression` веса модели хранятся в атрибуте `coef_`, а свободный член (смещение) — в атрибуте `intercept_`.

- **coef_**:
  - Это массив, который содержит коэффициенты (веса) для каждой из входных переменных. Для решения задачи с k признаками размерность этого массива будет (k,).

- **intercept_**:
  - Это скалярное значение, представляющее собой смещение модели; он добавляется к линейной комбинации входных признаков.

Полный набор атрибутов модели можно найти [здесь](https://scikit-learn.org/stable/modules/generated/sklearn.linear_model.LinearRegression.html).

---

Попробуем обучить линейную регрессию на числовых признаках из нашего датасета. В `sklearn` есть несколько классов, реализующих линейную регрессию:
* [`LinearRegression`](https://scikit-learn.org/stable/modules/generated/sklearn.linear_model.LinearRegression.html) &mdash; "классическая" линейная регрессия с оптимизацией MSE. Веса находятся как точное решение: $w^* = (X^TX)^{-1}X^Ty$
* [`Ridge`](https://scikit-learn.org/stable/modules/generated/sklearn.linear_model.Ridge.html) &mdash; линейная регрессия с оптимизацией MSE и $\ell_2$-регуляризацией
* [`Lasso`](https://scikit-learn.org/stable/modules/generated/sklearn.linear_model.Lasso.html) &mdash; линейная регрессия с оптимизацией MSE и $\ell_1$-регуляризацией

У моделей из `sklearn` есть методы `fit` и `predict`. Первый принимает на вход обучающую выборку и вектор целевых переменных и обучает модель, второй, будучи вызванным после обучения модели, возвращает предсказание на выборке. Попробуем обучить нашу первую модель на числовых признаках, которые у нас сейчас есть: